services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4321:4321"
    env_file:
      - .env
    environment:
      - DEPLOY_ADAPTER=node
      - HOST=0.0.0.0
      - PORT=4321
    restart: unless-stopped
